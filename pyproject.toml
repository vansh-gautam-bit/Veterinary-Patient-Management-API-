[project]
name = "vet-api"
version = "0.1.0"
description = "A FastAPI-based veterinary clinic management API for managing pets, owners, and visits."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "psycopg2-binary>=2.9.12",
    "pydantic-settings>=2.14.1",
]
